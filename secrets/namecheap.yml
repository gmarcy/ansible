---

namecheap_auth_client_ip: "{{ secrets_dir + '/namecheap_auth_client_ip' }}"
namecheap_auth_username: "{{ secrets_dir + '/namecheap_auth_username' }}"
namecheap_auth_token: "{{ secrets_dir + '/namecheap_auth_token' }}"
